import React from 'react'

export default function VoiceNoteList(/*{voiceNotes, user}*/) {
  return (
    <>
    {/* replace this with note component
    {voiceNotes.map(note =>(
      <div key={note.id} className='bg-gray-100 m-4 rounded-xl p-2'>

        <img src={user.profileImage} className='rounded-full w-7 h-7 bg-black inline-block'/>
        <span>{user.displayName}</span>
        <span className='ml-2 text-gray-500'>@{user.username}</span>
        <div>{note.title}</div>
        <audio controls src={note.audioUrl}></audio>

 
      </div>
    ))} */}

    </>
  )
}
